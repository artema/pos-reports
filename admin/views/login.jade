extends layout_base

block head
  title Login | SNAP Admin

block body
  .container: .row: div(class='col-md-4 col-md-offset-4'): div(class='login-panel panel panel-default')
    .panel-heading: h3.panel-title Please Sign In
    .panel-body
      form(role="form",method="post")
        fieldset
          if error
            div(class="alert alert-danger")
              a(class="close",data-dismiss="alert") &times;
              p
                if error.message
                  | #{error.message}
                else
                  | Unable to sign in. Please try again later.
          .form-group: input(class="form-control",placeholder="User name",name="username",type="text",value=encodeURIComponent(username || ''),autofocus,required)
          .form-group: input(class="form-control",placeholder="Password",name="password",type="password",value='',required)
          .form-group: button(type='submit',class='btn btn-primary btn-lg btn-block') Sign In
